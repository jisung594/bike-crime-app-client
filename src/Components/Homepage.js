import React from 'react'
import '../stylesheets/Homepage.css'

const Homepage = (props) => {
  // let { } => props;

  return (
    <div className="homepage">
      <h1>Dude, where's my bike?</h1>
    </div>
  )
}

export default Homepage
